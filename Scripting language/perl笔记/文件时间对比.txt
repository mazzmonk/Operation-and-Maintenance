#!/usr/bin/perl


#print 1900+$year,"\n";
#print "$yday\n";

#$date = sprintf "%02u/%02u/%02u %02u:%02u:%02u", $t[4] + 1, $t[3], $t[5] % 100, $t[2], $t[1], $t[0]; 
##$a=sprintf "%02u %02u %02u %02u %02u %02u %02u %02u %02u %02u", $t[0],$t[1],$t[2],$t[3],$t[4],$t[5],$t[6],$t[7],$t[8],$t[9]; 
#$b=sprintf "%02u",$t[7];
#print $b, "\n";

use File::Find;

($sec,$min,$hour,$day,$mon,$year,$wday,$yday,$isdat)=localtime;

$path="/home/blue/backup/www";
opendir DH,$path or die "cannot chdir to $path:$!";

for my $dir(readdir DH) {
        next if $dir eq "." or $dir eq "..";
        next if $dir =~/^\./;

        $full_dir="$path/$dir";
        if(-d $full_dir) {
#               print $full_dir,"\n";
                my @t=localtime ((stat $full_dir)[10]);
                if(($yday-$t[7]) > 7) {
                        print "$yday\t$t[7]\t$full_dir\n";
                }

        }
}


