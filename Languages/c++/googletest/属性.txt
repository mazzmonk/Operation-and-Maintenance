google Test assertions是一个类似函数调用的宏。用于测试类或者函数的断言。
如果断言失败，google test会打印断言的源文件和行数在失败消息里。可以提供自定义的失败消息。

ASSERT_*版本触发一个致命错误当失败的时候，并且终止当前函数
EXPECT_*版本不会触发致命错误当失败的时候，并且不会终止当前函数
通常情况下使用EXPECT_*，可以报告不止一个失败，但是，如果当失败时候不需要继续执行的时候，使用ASSERT_*
因为ASSERT_*从当前函数立即返回，可能会跳过后续的用于清理的代码，此时会导致空间泄漏，需要注意使用。
