apiVersion: v1
kind: Service
metadata:
  namespace: kube-system
  name: kubernetes-dashboard
  labels:
    name: kubernetes-dashboard
    version: v2.6
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 9090
    nodePort: 30000
  selector:
    name: kubernetes-dashboard
