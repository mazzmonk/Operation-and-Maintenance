apiVersion: v1
kind: Service
metadata:
  name: txxy-ofns-open
  labels:
    name: txxy-ofns-open
spec:
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31001
  selector:
    name: txxy-ofns-open
  type: NodePort
